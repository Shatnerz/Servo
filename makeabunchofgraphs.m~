figure(2)
hold on

load 'servo_band5.mat'
plot(temperature,'r.')

load 'servo_onoff.mat'
plot([zeros(1,400) temperature],'g.')

load 'servo_prop_band1.mat'
plot([zeros(1,400) temperature],'g.')
load 'servo_onoff.mat'
plot([zeros(1,400) temperature],'g.')

legend('ib1','ib2')

hold off